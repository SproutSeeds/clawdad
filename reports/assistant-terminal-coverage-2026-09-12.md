# Assistant-native Terminal coverage — September 12, 2026

Status: **Mac 0.7.0 (139) is installed, signed, notarized and healthy.** Both native queue forms passed exact-content, once-only and ordered-turn verification; the installed Assistant path passed after restart. Both disposable QA tabs are closed. iPhone 91 is already in the internal group from the speech lane; this Terminal repair requires no additional iPhone build. The local release catalog matches that verified Apple receipt.

Release sequence: Terminal checkpoint `564c53d` shipped in Mac 136. The speech lane's committed `3dd73e8` is preserved in the later Terminal packages; its Mac 137 candidate was held to avoid a downgrade. Selection checkpoint `6fa8d2b` shipped in Mac 138. Final activation follow-up is reserved as Mac 139. Terminal changes require no new iPhone binary; the independent speech release is iPhone 91, already verified by Apple as VALID / IN_BETA_TESTING in ClawDad Internal.

## Scope and preserved work

Cody authorized the Terminal adapters and their necessary repairs in this request. The earlier Terminal-control audit's implementation pause is superseded for this lane. Other computer-control expansion and icon reorganization remain discussion-only. No real project trust prompt, draft, queue or task is used as a destructive test.

Baseline: branch `codex/hermes-hybrid-supervisor-ui`, commit `5110956485d93c2158c31af5e154815bdea73ba3`, installed Mac 135 and iPhone 89. Installed and source MCP hashes matched at the start. Original worker `4301BB91-DC31-46DD-BE61-50CE7603DC61`.

Pre-existing dirty buckets are preserved: `.agents/skills/clawdad-release/SKILL.md`, `native/macos/build-app.sh`, `native/macos/package-release.sh`, `native/macos/storage-workflow.sh`, the integration plugin manifest and release skill, `assets/wordmark-explorations/`, `cloud/native/`, and `marketing-site/`. This lane owns the Assistant Terminal adapter/runtime/MCP/instruction changes, targeted tests and this report. Candidate evidence lives in the ignored `native/macos/dist/candidates/assistant-terminal-coverage-2026-09-12/` directory. No public CLI publication, new infrastructure or account login is included.

## Confirmed causes

1. Receipt `597430d1-bf9d-4070-a4c9-8fa040f202bc` began at 2026-09-13T01:12:16.933Z. It records `keySent=false`, `submitted=false`, `turnAccepted=false` and `enter-not-dispatched`. The native Enter guard requires an ordinary agent composer. It has no startup-choice adapter, so the authorized trust action was rejected before dispatch. The source is `MacAssistantTerminalInput.execute`, not a missing user instruction or a failed Enter delivery.
2. Reinspection found the same `agent-safety-lab` process/TTY already at an idle Codex composer. Its trust screen had been cleared before this audit's inspection. We did not accept its prompt or insert a research message. Exact original process: `codex-process-365527ce19027b71696b71c2598bc76475159aa9bbb3d56d0b1bc0772fd4a3cb`, TTY `/dev/ttys008`. Display names were never used as input authority.
3. The prior queue defects remain in baseline 135: the new-message queue route does not retain its own exact collapsed paste, and its final synchronous screen check uses a different normalization path from its async inspection. The patch retains verified paste provenance and uses one guarded async observation before dispatch, with explicit owner, working-turn, draft and binding failure codes.
4. The prior accepted-queue monitor looks up only the original catalog UUID. A worker/catalog rebuild can replace that display ID while the exact Codex process and session remain. Queue receipts now retain the process and TTY, request an exact read-only owner reconciliation, and keep the original request fingerprint intact. An unavailable historical receipt without enough identity remains for review.
5. Cold native selection returned AX `cannotComplete` (-25204) even when the exact tab was visibly selected. The old implementation rejected that result without observing the transition. The repair sends AXPress once and verifies the same native control, group and selection; it does not press again after uncertainty. Both before and after cases were reproduced on disposable tabs.
6. Selecting a native tab did not establish application keyboard focus. Mac 138 inspection `216ba233-d934-47c8-ab16-833fb215438e` failed generically; the following `56c364b6-3da9-4a4d-956e-7e1a8fd4b2fc` said to focus the prompt. Native observation then found Finder frontmost, AXOutline focused, while Terminal's exact target was selected. The cold path previously made an unchecked NSRunningApplication activation request. It now uses the same authorized Terminal activation Apple Event as the existing TTY route, waits briefly for observed activation, and reports `terminal_activation_unavailable` if that fails. [Apple documents activation as a request, rather than a guarantee](https://developer.apple.com/documentation/appkit/nsrunningapplication/activate(from:options:)). The live repair moved Finder → Terminal and verified the exact fixture native tab and `/dev/ttys013` input in 677 ms, without typing.
7. A sample during busy output showed repeated synchronous title callbacks doing whole-window inspection. Title events are now coalesced and bounded. This reduces that observed work; it does not prove that the title callback explained every XCTest harness stall. Failed harness attempts remain in the evidence directory.

The combined inspection guard now distinguishes unresolved catalog metadata, manual-input changes and missing keyboard focus. Queue diagnostics distinguish unreadable input, changed draft, missing Tab binding, changed owner/working turn and uncertain post-Tab acceptance. An opaque draft does not make an observed Tab binding disappear: capability and current readiness are returned separately, including readiness for a new message versus an existing draft.

## Implemented interface

`inspect_terminal_input` reports a separate observed `prompt`, including its kind, question, choices, selected choice, directory and SHA-256 prompt identity. `respond_terminal_prompt` receives the exact native input token, prompt ID, choice ID and a quote of Cody's actual authorizing message. An earlier user request in the same conversation can supply that authorization; no repeated confirmation is required. The service validates that provenance and stores its request ID and hashes before native selection/Enter dispatch. Repository text, agent output and caller-supplied authorization objects cannot supply that provenance. The Assistant must still interpret scope, later restrictions and user-approved AGENTS rules correctly.

Prompt decisions, Enter message submission and Tab queueing have distinct receipts. Prompt dispatch is prepared durably first; selection changes are observed before Enter. The native result distinguishes `decisionSent`, `resultVerified`, and `readyForAgentInput`. It always keeps `submitted=false` for prompt decisions. An uncertain result never causes an automatic repeat. Credentials, unsupported prompts and ambiguous inputs retain their user/platform flow.

The live Codex 0.154.0 trust screen places `> You are in <directory>` before a paragraph beginning with the trust question. The parser covers that actual rendering, canonicalizes verified filesystem path aliases such as `/tmp` and `/private/tmp`, and hashes the observed prompt separately from its selected row. It does not infer ownership from the displayed path.

## Capability inventory

| User action | Assistant-native path | Verification / boundary |
|---|---|---|
| List/read/refresh Terminal windows and tabs | `workspace`, `inspect_tab`, `observe_tab` | Exact native catalog, foreground process and session; names are display metadata |
| Inspect/rebind native shell or fresh agent input | `inspect_terminal_input` | One-use 45-second token; optional prior TTY/input-session/foreground identity tuple |
| Accept/decline directory trust | `respond_terminal_prompt` | Exact observed directory question and choice, user-message authorization, prepared receipt, observed result |
| Numbered menus and canonical yes/no prompts | `respond_terminal_prompt` | Explicit observed selection/footer; canonical echo required for line input; result must reach another recognized prompt/composer or shell |
| Type into an ordinary shell | `type_terminal_input` | Exact readable shell draft and foreground process, no Enter/Tab |
| Prepare a project launch | `prepare_project_launch` | Separate directory and Codex drafts; safe quoting and verified directory |
| Insert a Codex draft, idle or busy | `insert_in_tab` | Exact process/session, native paste and visible/readback provenance |
| Clear, replace or append draft text | `clear_tab_input`, `replace_tab_input`, `append_to_tab_input` | Fresh expected draft; whole-draft authorization for opaque collapsed input; images remain protected |
| Submit an idle Codex draft | `press_terminal_key` with Enter/submit, or `send_to_tab` | Native prepare plus exact new owning-rollout user turn, independently of key dispatch |
| Queue new text or an existing draft while busy | `queue_in_tab`, `queue_tab_draft` | One Tab, active-turn/owner/draft/binding checks; rendered queue or exact later accepted turn |
| Special keys and custom navigation/edit combinations | `press_terminal_key` | Explicit action intent; supported Shift/Option/Control chord adapters; guarded Shift-Left queue recall |
| Create/select/reorder/rename/close tabs | Existing dedicated Terminal tools | Native identity, catalog revision and applicable close authorization |
| Save/update/restore named setups; guarded window close | Existing Main Workspace tools | Durable snapshot revisions, exact window and separate save/restore/close requests |
| Images, clipboard, selected/latest response and pointer operations | Existing image/clipboard/context/pointer tools | Existing native target, permissions, draft and attachment protections |
| Unsupported REPLs, passwords/sign-in challenges, arbitrary macros/bindings | Explicit unavailable result | No generic Computer Use or blind key fallback |

### Other computer-control gaps for discussion

The existing `computer` tools cover authorized app inspection/capture, supported pointer/key/text controls, launch and readable accessibility text editing. Phone-local preset editing, photo-library selection, iPhone clipboard operations, microphone/call consent and phone navigation stay on the device. Unsupported secure/read-only app inputs and unverified application-specific workflows need dedicated adapters. Broad generic input is not an alternative Terminal transport. No expansion of these surfaces is included here.

## Verification evidence

- **Final JavaScript suite: 719/719 pass**, zero skips or failures (`released-139-js-tests.log`). The final focused transport/release/registry rerun also passed 30/30. Coverage includes actual MCP/authenticated HTTP serialization, genuine prior-message authorization, missing authorization, restart uncertainty, duplicate IDs and exact catalog rebind.
- Final source native suite: 327 tests, 17 opt-in tests skipped, zero failures. The separately enabled live tests exercised actual native controls and MCP/HTTP transport; skips are not counted as live proof.
- An isolated Assistant service and the actual native bridge operated disposable tabs with separate history and workspace storage. Routine background workspace/activity observation was excluded from this harness; the production default remains enabled. The installed Mac's real MCP path independently inspected and exercised the same disposable process. Earlier harness focus/stall attempts remain in the evidence directory. The two specifically reproduced selection/activation defects have their own before/after evidence above; unrelated harness stalls are not claimed fixed.
- Accept: receipt `130e95ec-eb5b-4335-bbaa-f1baeefa0a38`, prepared 02:04:25.378Z, observed result 02:04:25.862Z. One Enter; trust accepted; no conversation turn submitted. Repeating the same request returned the same receipt with no dispatch.
- Decline: receipt `5a24ae2a-d062-4bcd-a92c-33187c9c4e67`, prepared 02:06:49.227Z, observed result 02:06:49.864Z. One Down followed by one Enter; return to ordinary shell verified; no conversation turn submitted.
- Canonical yes/no and numbered-menu decisions ran through actual MCP → authenticated HTTP → native bridge with isolated durable state. Their chosen values and return to shell were observed, and duplicate request IDs returned original receipts. Evidence: `live-line-decision.json`, `live-menu-decision.json`.
- Expanded/long collapsed Unicode drafting and separate Enter submission ran against disposable Codex 0.154.0. `8e85507b-ec24-4019-875e-4758c583c041` matched the complete intended text to its accepted owning-session turn; Enter dispatch alone was not treated as submission. Busy replacement, append and clear passed. One earlier replacement stopped before mutation when the composer rendering changed; a new explicit whole-draft replacement reconciled the disposable opaque paste. Another queue attempt stopped because the working turn had actually completed; no Tab was sent. These preserved failures are distinct from the repaired queue defects.
- **Both native Tab forms passed actual accepted-turn verification.** Existing-draft queue `8105ed95-e800-407c-94e5-5bbae3ff3aac` delivered 2,577 UTF-8 bytes into turn `01a098b4-4d49-7ab2-92d5-1af8626504ea`. Insert-and-queue `1ea29613-40f3-427c-b437-6c25a61cff83` delivered 2,512 UTF-8 bytes into turn `01a098b4-5858-72c1-a3ae-8960ef4b9cec`. The full-text SHA-256 values match their owning rollout exactly. Each text occurs once, and the distinct turns executed in order after the baseline task. Repeated original request IDs dispatched no additional Tab. Initial clipped queue rendering was honestly recorded as uncertain; later accepted-turn reconciliation completed those same receipts without resending. Decisive artifact: `queue-final-verification.json`.
- Owner for these disposable queue tests: session `01a09882-3832-70e1-8841-6f1df792aa37`, TTY `/dev/ttys013`, process `codex-process-c2caa46a5e875437606cd85026c7f91432cd555d82f6005a00a8cb7c20d063e4`. The real safety-lab and research threads received no test messages.
- Regression coverage includes absent/earlier genuine user authorization, changed prompt/selection/process, ambiguous/secure prompts, request reuse/conflicts, worker restart after preparation, token expiration/one-use validation, shell versus agent ownership, Unicode and collapsed paste provenance, active-turn transitions, queued-item preservation, stale catalog UUID reconciliation and no Enter fallback for a requested Tab.
- Prior full test runs exposed unrelated flaky temporary cleanup and shell-registry fixture races; targeted reruns passed. The concurrent iPhone 91 release also left the release catalog at 89. The final checkpoint reconciles the catalog to the Apple 91 receipt and updates the pinned release assertion, preserving the assertion that project and catalog builds agree. No release test was weakened.

### Operational limits

An instruction already given by Cody can authorize the corresponding supported prompt choice; the Assistant should cite that existing user message internally instead of requesting it again. Choosing trust permits the directory's local configuration/hooks/policies to load. The Adapter never derives a new authorization from those files, agent output or a prompt's own wording. Scope interpretation remains an Assistant responsibility under user-approved AGENTS rules and higher-priority constraints.

Generic Enter remains the wrong transport for a trust/menu decision: inspect, then use `respond_terminal_prompt`. Missing user authorization, stale identity, expired inspection, secure/unsupported input and uncertain delivery have separate recovery paths. Unsupported raw-mode REPLs, unrecognized bindings and credentials are reported explicitly. An accepted queue entry or uncertain dispatch is reconciled before retrying; an expired inspection that sent no key can be safely replaced with a fresh inspection.

Other application-control gaps are inventory-only. No additional general Computer Use capability, account login, cloud resource or permission grant is introduced by this release.

## Installed release and final reconciliation

- Source checkpoints: `564c53d` (prompt/queue/provenance), `6fa8d2b` (observed selection and queue diagnostics), `f71bee1` (verified iPhone 91 catalog), `a7cf5b9` (observed activation and specific inspection diagnostics). Committed speech checkpoint `3dd73e8` is included and preserved.
- Mac 139 installed at `2026-09-13T03:23:28.541167Z`. Executable SHA-256 `842bb96f6aa03ff9608e7ef3018d2c004274eca9aee6058306b8cea59329d040`; embedded/loaded runtime fingerprint `f3f229d01950d9913334c53bca4ac86db531faf8aae600ed83a631995baebdec`. Installed runtime source hashes match. Service/native readiness returned in 10.912 seconds after app restart. Project API, cloud status and speech readiness were healthy.
- Apple notarization: app `201d41eb-ef0b-49ef-b369-2823aee306aa`; DMG `c79ab5f7-2b5b-4df5-9f05-8f073ad01e8c`; both Accepted, stapled and Gatekeeper accepted. Signed archive/DMG and rollback app remain in the candidate directory. The generated appcast is local packaging output; it was not published as an unrelated public release.
- Installed native inspection `f81e7381-aa5d-44e8-9716-68be56f57573` and agent inspection `aecb877b-daad-4674-9339-930dc0f269f9` verified exact session/process, usable agent input and empty draft after restart. The initial successful cold native inspection is retained separately. One harness assertion compared `/tmp` with its `/private/tmp` alias; the fixture assertion was corrected to compare real paths without changing product behavior or sending input.
- Both completed native queue receipts were reconciled again on Mac 139. Original IDs, exact content hashes and one occurrence each were preserved; no work was replayed.
- Disposable close `cc9e6e81-7aef-44a2-9982-a6e026ead2cf` returned the expected native process confirmation. The harness initially expected a completed status instead of the correct attention/confirmation state. Its already-authorized cleanup decision was then resolved through the single-use native token. The exact finished QA agent and the ordinary QA shell both closed, with no whole-window close. `cleanup-state.json` contains each receipt. The isolated QA service was stopped.
- Installation verified unchanged Assistant history, user-authored Assistant instructions, research state, account budget preferences and all 18 Terminal agent processes present immediately before that install. QA cleanup subsequently closed only the independently identified fixture. Real research tasks, drafts, native queues and saved workspace membership were not used as fixtures.

## Handoff and preserved buckets

The Terminal source, tests and evidence report are checkpointed separately. Pre-existing release/storage workflow, plugin workflow, branding, infrastructure and marketing paths remain intentionally dirty for their owners; their exact paths are listed under Scope. They were not included in the product commits. Candidate evidence and rollback packages are canonical local artifacts; the temporary release worktree is removed after verification. Final `git diff --check` and ORP hygiene must show no unclassified paths.

Remaining hands-on checks: issue a supported trust/menu instruction from the physical iPhone by voice and by text, confirm the expected target/result, and exercise existing touch/VoiceOver navigation. The installed native tests establish dispatch/receipts and observed Mac results, not iPhone recognition accuracy or physical accessibility behavior. No physical power-cycle, logout, real Terminal-window closure, or live-project trust decision was performed.

Physical iPhone voice intent recognition and a phone-originated trust decision remain distinct device checks. Automated and native desktop tests do not prove iPhone microphone accuracy or physical touch behavior. No reboot, logout, real Terminal closure or power-loss test is performed.
