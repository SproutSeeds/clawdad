Proposed next implementation: clear desktop Save/Open Terminal setups

This is a review prompt, not authorization to execute it yet. Read reports/desktop-terminal-workspace-audit-2026-09-13.md and reconcile current source, builds and saved schema before implementation. Preserve unrelated changes. The named-snapshot engine and both client paths already exist; reuse them. Keep the implementation bounded to desktop discoverability, accurate capture/restore preview and the specific concurrency/identity guards needed for that UI.

Replace the existing low-emphasis “Main Terminal Workspace” home-screen link with a compact Terminal Setups row above the project composer containing “Save Terminal Setup…” and “Open Saved Setup…”. Preserve the current project/session composer and per-message model controls. Use the existing embedded desktop UI and native workspace actions rather than creating a second storage system or rebuilding Settings. Native menu shortcuts are optional follow-up, not required scope.

Save opens a focused sheet to choose one exact physical Terminal window and give its snapshot a name. Show the window’s tab count and left-to-right lineup, each tab’s display name, verified actual directory, Codex conversation versus shell type, expandable exact session ID, and draft/identity limitations. A single live window may be preselected only with its preview visible. A window title or matching project directory is never routing authority. Do not silently switch selection when a representative tab/catalog ID changes. Bind the reviewed window/members to native capture using the existing stable ownership metadata or a narrow verified descriptor extension. If the window or lineup changed, preserve the proposed name and ask for a refreshed review rather than capturing another window.

Save as new captures exactly that window. An explicit Update of a selected setup replaces its lineup, presents removed/changed agent-to-shell entries for review and retains its previous version. Handle duplicate names clearly without implicit overwrite. Preserve all existing legacy records, previous versions and receipt history. Show home-directory shells and temporary fixture paths accurately; never infer a missing agent from a title or silently clean Cody’s current snapshot. Save can preserve a known limitation, but must never imply that hidden drafts/attachments or shell-only tabs are fully resumable agents.

Open lists named setups with capture time, count and identity/draft warnings. Selecting one previews it without restoring anything. Open/Restore must bind the selected snapshot ID and reviewed snapshot revision. Add a narrow revision check so another client cannot change the lineup between preview and dispatch. Accepted operations and retries keep their original durable request identity; an intervening update must not silently reinterpret an uncertain request. Keep the existing global update lock/revision protections and durable per-snapshot creation/launch reconciliation.

Use the existing ownership policy: reuse exact live Terminal owners, create only confirmed missing tabs, and resume the exact saved Codex UUID in its actual directory using latest persisted history. Distinct sessions sharing a directory remain distinct. A setup split across windows or a thread held by an app-server/other runtime requires explicit actionable recovery, not a competing process. “Open a separate window” may create a window for missing independent conversations; it must not clone an already-running session. If another unrelated setup is open, offer the supported reuse/separate choice and preserve all unsaved tabs. Existing live drafts win. Restore does not send “continue,” replay work/queues, enable supervisors or promise recovery of an in-memory computation.

Keep regular-window usable-display sizing, including menu/Dock space and Terminal grid rounding. Preserve per-tab success when other entries wait for a drive, exact directory/history, executable, trust/sign-in, permissions, or ownership. Show durable progress and a clear check-receipt/retry-waiting action. Leaving the sheet must not cancel accepted restoration. Preserve supported saved drafts, mark opaque content and pending/uncertain receipts for review, and never infer hidden text.

Opening a Save/Open sheet should begin with read-only canonical status, not automatically enqueue a native writing inspect/migration command. Use an explicit fresh-window inspection path when needed. Preserve request IDs through navigation/reconnect/restart; test desktop localStorage’s loopback-origin limitation and use existing stable native persistence if necessary. Keep the phone on the same canonical library and versioned protocol, with only narrowly necessary compatibility changes. No new cloud storage or broader settings architecture.

Give both sheets Done/Cancel, Escape, restored focus, accessible button names and comfortable targets. Keep Close window out of these primary flows. Save never authorizes close. The existing separate guarded close feature may remain available but must not be invoked by browsing, Save, Update or default restoration.

Before delivery, verify manual snapshot immutability after background/empty/home-shell observations and simulated restart; exact Save/Update replacement; two separately named physical windows; stale window IDs and membership changes; phone/desktop concurrent revisions; same-directory distinct sessions; fresh/exited/missing-history agents; latest persisted history after Save; regular-window sizing; draft and receipt preservation; missing drive/owner conflicts; double clicks; uncertain creation/launch and restart reconciliation; and no duplicate accepted work. Verify a desktop Save/Open flow through the actual native Assistant transport using isolated fixtures, beyond mocked UI assertions. Check compact/wide desktop and small/large iPhone shared-state layouts, keyboard and VoiceOver behavior. State remaining physical iPhone and power-loss checks honestly.

Testing safety is mandatory. Start with isolated file/model fixtures and read-only real-state observations. No test may close, replace, enroll or restart Cody’s real tabs/window or invoke a real reboot/logout/power-cycle. Any later live close fixture must have a dedicated isolated context and an independently verified allowlist of its physical window, every TTY/login/process and fixture directory. Abort on any unrelated member, ambiguous identity or manual activity; never fall back to whichever window is focused. If such isolation cannot be established, leave that live close test unperformed and report it. Do not modify the current real snapshot’s temporary test entries or home shells as cleanup.

Report the scoped changes, source/installed-build evidence, exact test receipts and history/identity checks, remaining limitations and physical-device checks. Keep the nine unrelated dirty buckets classified. Follow the native release workflow only after this proposal is approved for implementation; do not publish an unrelated CLI or make infrastructure changes.
