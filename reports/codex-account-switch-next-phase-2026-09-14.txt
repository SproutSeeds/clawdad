Implement the reviewed first phase of deliberate Codex subscription-account switching in ClawDad. Read reports/codex-account-switch-audit-2026-09-14.md and the complete received prompt beside it. Reinspect installed versions, effective authentication policy, current native/service ownership, and repository hygiene. Preserve all unrelated work. This prompt is a proposal until Cody approves it; it does not authorize a live account change by its mere presence in the repository.

September 15 clarification takes precedence: also read reports/codex-account-switch-discussion-2026-09-15.md. Remove the automatic global 20% research reserve; offer explicit optional limits only for selected projects/supervisors. Target one-click selection between securely retained authenticated Codex accounts after initial setup, while keeping that capability gated until supported storage isolation, refresh and session transitions are verified. Saved passwords assist user-owned reauthentication; they do not substitute for Codex authorizations.

Scope: OpenAI subscription authentication only. Build a deterministic native/service switching controller, account information and preview/recovery UI, and meaningful offline fault tests. Keep real authentication/transition execution feature-gated until the separately authorized two-account experiment establishes supported behavior. No CLI publication, new infrastructure, API-key fallback, credit purchase, reset-credit redemption, or account cycling. No new AI dependency.

1. Extend the existing weekly allowance information popover on phone and Mac with verified Codex email, entitlement/workspace identity and name when available, subscription method/plan, weekly percentage/reset, last refresh, explicit stale/unavailable states, and an account selector. Keep ClawDad pairing identity separate. Do not invent a workspace name or use an email as a stable entitlement ID. Preserve current 5%/0% informational alerts and deliberately configured project limits; remove the global research reserve as specified below. Propose an optional switching reminder for review without adding a stopping threshold. Use fresh backend ordinaryUsageAllowed/reached-limit and short-window evidence where available; keep unavailable fields unknown on older runtimes. Add account should use supported initial sign-in; selecting a retained valid authorization should perform the full safe switch without redundant login. Show a clear reauthentication path only when required.

2. Introduce a private, atomic, revisioned switch journal on the internal drive, independent of manually saved Terminal setups. One durable operation/request ID and credential-realm lease must coalesce repeated clicks, reconnects and controller restarts. Write before each side-effect boundary, record observed verification and uncertain receipts, and reconcile before retries. Keep credentials, browser cookies, device codes, OAuth URLs/query parameters and recovery secrets out of journals, diagnostics and source control. The controller and its UI must operate when every model call is disabled or exhausted.

3. Build an exact read-only preview of affected credential consumers: standalone Terminal Codex owners and descendants, shared/isolated app servers, main Assistant exec children, research reviewers and allowance readers. Preserve original ownership transport. Record exact window/tab/order/name/selection, login/process start identity, cwd/worktree, UUID/history path and last accepted message IDs, model/effort, draft generation/hash/provenance, supported image files/hashes, queue IDs/order and pending receipts. Flag unavailable/fresh/no-ID/ambiguous states. Do not call manual Save or restore a saved setup to obtain switching state. Keep shells and normal usable-display window sizing unchanged.

4. Add a durable account-transition admission fence at every ClawDad dispatch/review/launch boundary, with an account epoch on new accepted requests. Existing unbound requests need observed reconciliation, not guessed attribution. Wait for active turns and accepted native queues by default. Hold local waiting deliveries without changing their IDs, content, settings or previous account authorization. Preserve manual pauses and stopped supervisors. A shared credential write must be blocked while any affected consumer is busy, unrecoverable or unknown unless actual isolated credentials have been verified; calling a session deferred does not isolate it. Manual activity after preview invalidates the affected approval/inspection and triggers a precise recheck.

5. Use supported managed browser/device-code login only. Do not reuse MacSystemReadiness.openCodexLogin's existing unconditional daemon restart or reset/logout sequence. Audit the existing sign-in entry points so they cannot bypass an in-progress switch fence. Honor managed login/workspace requirements. Both installed schemas mark chatgptAuthTokens internal-only; do not use it to implement a credential vault. Do not manually rotate auth.json files, duplicate refresh tokens, or claim configuration profiles are authenticated-account slots. Secure multi-account storage and destination-first staging require separate evidence about storage isolation and supported token lifecycle.

6. Authentication steps must be explicitly classified: deterministic CLI/app-server code for login creation/poll/cancel and callback verification; supported browser handoff for opening the returned URL; user-owned account/workspace selection, consent, MFA/passkeys and device-code entry. No AI computer-use fallback in this version. If destination staging cannot be proven isolated, first drain and fence the shared credential realm, then authenticate without logging out in advance. Never claim that cancellation means credentials definitely stayed unchanged. Validate login ID, method, exact principal/workspace, supported models and fresh allowance; a success page or login status is insufficient.

7. Implement transition adapters only after the disposable experiment proves them. Prefer verified hot reload where supported. Otherwise exit only eligible idle Codex owners, retain their Terminal shells/windows/tabs and resume the exact UUID in the exact directory with the recorded model/effort. Do not create replacement threads or switch between Terminal and app-server ownership. Verify latest persisted history, unique ownership, account attribution and restored draft bytes. Opaque pasted text, unsupported images, unresolved queues and uncertain accepted work block destructive transition. Restoring drafts never presses Enter or Tab; restoring a conversation never sends continue. In-flight computation is not restored from a thread ID.

8. Remove the automatic global 20%-remaining research pause throughout defaults, admission logic, schema, UI, Assistant tools/instructions and help. Default to an explicit no-project-limit state, rather than hiding a global rule behind a zero threshold. Allow Cody to add/remove a stopping percentage only for the exact selected project/supervisor scope through the Assistant or manual controls. Preserve deliberate project limits and their account scope, historical ledgers/authorizations/alerts, manual pauses/stops, objectives, drafts and accepted deliveries. Retire default-only global latches with a recorded migration; do not launch work or automatically resume a stopped supervisor during migration or account switching. Destination accounts must not acquire a new automatic reserve. Do not reuse another account's grants or exceptions. Any future supervisor account rebind remains deliberate and revisioned. Retain fresh account/usage evidence and uncertain-delivery protections. Verify whether backend accountId identifies the principal or workspace allowance scope before extending ledger identity; do not guess a migration. Already-running work may consume more before it finishes.

9. Reconcile login cancellation/failure/expiry, wrong browser account, lost callbacks, network loss, host restart, unsupported destination models/policy and partial restoration. Retain a mixed-account/needs-attention state until every affected session is verified. A rollback is verified source-account reauthentication unless a supported isolated credential slot has been proven; do not silently replace cached tokens. Keep recovered sessions intact while retrying only confirmed missing operations. Do not replay tools, native queues, accepted messages or uncertain launches.

10. Run offline fixture tests before any account operation: duplicate IDs/clicks; stale preview and process identities; multiple same-directory conversations; shell-only tabs; expanded/collapsed/Unicode drafts and images; no-first-turn agents; manual edits; locally waiting versus native accepted queues; source exhaustion/401/429; account change during read; two-account budget isolation; no automatic pause merely at 20%; deliberate project limits; default-only latch migration versus manual pauses; no hidden reserve for new/destination accounts; stopped state; canceled login and lost callback; fault at each write/commit/resume boundary; model unavailability; zero model transport; unchanged manual snapshots and history hashes. Test actual shared/native request paths using mocks/disposable services with no credentials, rather than asserting source strings alone.

Before the live experiment, return the exact proposed source/destination accounts/workspaces, isolated credential environment, disposable processes/sessions, synthetic history/images and actions for Cody's explicit approval. No real ~/.codex account switch is authorized merely because a test Terminal tab is disposable. Prove isolated storage first; if that cannot be established safely, request a separately authorized macOS test user or test Mac. Do not log out, restart production services, stop real agents, accept real trust prompts, send research tasks, or spend down allowance for the experiment.

Live acceptance, once separately authorized: demonstrate A→B→A browser/device login where enabled; observed credential-refresh behavior for each supported runtime; exact UUID/history/settings preservation; unique owner and unchanged Terminal topology; complete recoverable draft/image preservation; exact message identity/order with no duplicate accepted work; restart/reconnect recovery; correct allowance and policy attribution; unchanged named snapshot fingerprints; and manual recovery without any model call. Stop on a real workspace/data-access restriction. Distinguish documented, schema-confirmed, offline, live native and physical iPhone evidence.

Deliver scoped reviewable changes and a concise capability matrix. Do not enable an unverified real-switch path or release a claim of complete preservation based only on mocks. Coordinate any production transition/release after the live acceptance gates, preserving all existing Terminal tools, call/chat controls, speech, drafts, images, histories, workspace saves, notifications and research protections.
