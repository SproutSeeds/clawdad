Proposed next implementation request — for Cody's review; not authorization to execute

Read reports/assistant-first-chunk-audit-2026-09-12.md and reconcile the installed iPhone/Mac builds with speech commit 3dd73e8 and the volume-boost lane. The phone was still on build 90 during the audit. Build 91 already contains the finished-fade correction and is available in ClawDad Internal. Preserve that repair and avoid duplicate implementation or a new release merely to ship it again. First verify the installed phone version and coordinate Cody's replay of NASA reply assistant:3da57e05-cee1-47b2-8b9e-9630ec5a217a:item_0 on 91 or a later verified build.

After approval, harden the native playback completion path against an engine that reports running while its clip stops advancing. Use measured startup/output grace and monotonic native playback progress, accounting for intentional pause, interruption, app suspension and route changes. A long clip must not fail just because its total duration exceeds a short wall-clock limit. Resolve the exact owning continuation once on a verified stall, with the last observed position. Retire/reset only the failed output slot so bounded retry cannot reuse the stuck state.

Reuse the existing consistent-voice recovery controller. Preserve immutable current audio, part and batch index, exact remaining text, playback offset, selected engine/model/voice/speed, gain and limiter behavior. Retry briefly or expose Resume and Stop; never silently change voice, skip a part, replay a completed prefix or let late recovery restart stopped/replaced playback. Preserve microphone consent and mute state, call connection, typed/image drafts and other ongoing work.

Add bounded privacy-safe playback diagnostics with installed build, automatic/manual origin, per-attempt and generation IDs, native progress/duration, output-engine state, callback result and specific pause/cancellation reason. Retain bounded prior-run evidence without text, recordings, microphone samples, URLs, credentials or noisy chat cards.

Promote incident-specific disposable regressions into the appropriate maintained tests: three consecutive same-rate native chunks including a long second chunk; automatic and speaker-button controller playback; startup and mid-clip zero progress; second synthesis/download failure and exact suffix recovery; retry after cache reopen; background/foreground and interruption distinctions; cancellation at the chunk boundary; repeated replay and replacement with stale callbacks. Verify that progress actually advances and completion is observed, rather than treating a play request as successful output. Reuse the saved NASA WAV hashes and synthetic fixtures. Avoid production synthesis side effects, microphone activation or interference with Cody's current playback during tests.

After verification, follow the established native release workflow only for genuinely new scoped changes. Reconcile the volume lane, preserve unrelated work and report exact delivered/installed versions. Keep physical iPhone complete listening, route/lock-screen behavior and audible recovery separate from muted native, automated and simulator proof. Do not restart or update Cody's phone, alter his microphone state or release anything merely on the authority of this proposed prompt; wait for his approval of the implementation phase.
