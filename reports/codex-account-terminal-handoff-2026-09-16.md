# Codex account handoff implementation checkpoint — September 16, 2026

Status: implementation and verification in progress; **not released**. This report extends `codex-account-runtime-layout-2026-09-16.md`. The production complete-switch capability remains guarded until the native and managed-runtime integration is verified.

## Confirmed progress

- Both approved subscription authorizations remain independently retained in Keychain at their existing homes. No sign-in or credential change was performed in this continuation. The 07:53–07:56 UTC account-only checks observed Cody falling from 5% to 4% weekly remaining and Sun at 0%; these checks submitted no model turns. These are timestamped readings, not a promise of current allowance. Sun's exhaustion limits a new model-response test, not the deterministic switching controller.
- A real CLI round trip, Cody → Sun → Cody, passed in a private persistent zsh/PTY. Each resumed the exact synthetic conversation `01a0a848-cb86-7033-ae6f-ce006f5b51bb`, preserved the original accepted history, gpt-6-astra/low and read-only/never policy, and restored the same 3,438-byte unsent Unicode/multiline draft. No Enter or Tab submitted that draft. A repeated request did not repeat the transition.
- The draft SHA-256 was `9597d99f5aeb0bfbfdcf8182439a1eebe1fa41dd2c9bca8d726d86c10c7cd771`. Verification used retained exact native paste plus the collapsed representation, rather than claiming independent readback of hidden text. The accepted-event history hash was `f4b0d154d981f4148e87f86aa26ba74c09bb6dc71aabc9391aff3b4599d44d00`; this hashes a different projection from the earlier complete-message hash. Both before/after values matched.
- The round trip ran 06:06:23.916–06:06:31.839 UTC on September 16. Both destination processes used the same private `/dev/ttys015` and persistent fixture shell, PID 44269. Cleanup and a separate read-only history recheck completed. These are CLI/PTY results, not a claim of production Terminal-app adoption.

## Implementation in this checkpoint

- Inactive profile layout adoption retains original files and links in a private, durable recovery journal. It refuses active/uncertain processes, changed source resources, foreign permissions, substituted backups and divergent configuration. A per-profile launch gate prevents managed account-check processes starting during migration. Rollback preserves links that predated adoption. Actual retained profile layouts have not been migrated to production history.
- A read-only native process census includes background Codex owners without exposing environment secrets. The actual census observed 19 canonical-home owners and zero Sun-home owners at that observation. Results are nonce/worker/home-bound and expire quickly; they are not permanent inactivity assertions.
- A durable per-tab controller records stop, exact resume and draft restoration separately. It requires exact tab lifetime, physical window, original shell, process, conversation, project, accepted history, account/settings and recoverable text. Busy work, native queues, unresolved receipts, attachments and opaque text pause before termination. Unknown delivery is reconciled rather than resent.
- A separate private native mailbox serializes these controls with ordinary Assistant-native work. Only the controller can enqueue requests; public Assistant tools cannot manufacture a switch. Each worker must prepare the exact request durably before dispatch. Worker changes before preparation prove non-delivery; changes after preparation retain uncertainty. Late identical receipts are idempotent.
- The native adapter uses the existing Terminal focus, captured-input, paste, shell-entry, permission and manual-input paths. It preserves a private draft recovery record before local `/status` inspection, requires an idle parent shell that survives Codex exit, resumes by exact UUID and directory, and keeps routing environment changes child-scoped. It does not create or close production tabs or write manual workspace snapshots.
- New-owner attribution requires the launch request marker and its private native receipt. Historical origin metadata does not substitute for current ownership. Reading `/status` is a local command, and accepted-history comparison checks that no model turn was created.

## Verification and failed approaches retained

The latest consolidated runtime run passed **834/834** checks (`account-routing-full-runtime.tap`). A later same-account/original-home regression passed with all **12 Terminal handoff checks**. The consolidated native suite passed **46 checks with six explicit live skips (52 total)**; the subsequent actual native fixture compiled and exercised the newest exact-selection and shell-launch path. Eight new durable shared-handoff controller checks passed separately. No physical iPhone verification is implied.

Evidence lives under `native/macos/dist/candidates/codex-account-switch-2026-09-15/`. Private actual-account and TUI artifacts remain under `~/Library/Application Support/ClawDad/Accounts/verification-2026-09-15/`; credentials are not included in these reports or receipts.

- `handoff-pty-1`: failed before Codex launch because `ttyname` was called on the master PTY. Removed that unsupported lookup and strengthened fixture cleanup. No model/login action occurred.
- `handoff-pty-2`: successful same-PTY round trip described above; follow-up six-history read-only checks also passed.
- `native-handoff-1` and `native-handoff-2`: actual Terminal-app observation stalled in focus before Codex launch. Test-worker diagnostic traces were added; only those identified test workers and exact empty, one-tab QA windows were stopped/closed. No production window was closed. The first fixture also exposed a missing no-op coordinator `stop` in its test harness, now supplied.
- `native-handoff-3` and `native-handoff-4`: native observation returned a safe shell-identity refusal. Live `ps` output had extra whitespace before the final executable field; the new parent-shell parser did not trim that field. Corrected the parser and added a regression matching the real output. The same empty fixture window was reused for the next attempt.
- `native-handoff-5` through `native-handoff-13`: preserved failures exposed actual-owner history lookup, animated composer frames, full-catalog rescans, local command exit, TOML slash escaping and duplicate model flags. The account adapter now uses the owning process home, exact selected native input checks, stable composer frames, native `/quit`, unescaped TOML path quoting and one verified model/effort argument. Every affected disposable fixture was reconciled individually; no real tab/window was closed.
- `native-handoff-14`: successful actual Terminal/native-HTTP Cody → Sun → Cody round trip, 07:35:55.173–07:40:08.001 UTC, window 12758 and TTY `/dev/ttys015`. Both resumed the exact original synthetic thread. A live open-file observation verified the intended shared SQLite index.
- `native-handoff-15`: after the exact-selection guards were strengthened, a redundant full-catalog shell inspection stalled before launch. Its timeout retained uncertainty without retrying. Exact empty fixture/worker cleanup was independently verified. Account launch now reuses the already verified native selected input while retaining the normal shell/token/owner checks.
- `native-handoff-16`: repeated the real round trip after that repair, 07:53:07.412–07:57:36.116 UTC, exact one-tab QA window 12766, same TTY, Sun PID 71783 then Cody PID 75673. Each retained model `gpt-6-astra`, effort `low`, exact conversation, policy and the **2,179 UTF-8 byte / 2,141-scalar** unsent draft. Draft hash `50cc2f2c82af6e889aec26ce4ab048716054526d899ce13ee1aa086a3a5d96f2`; accepted-history projection before/after `acc6ab99e0f6d046fab8759215a99025c32175e7bb51c117195255fb3f116c8b`. Zero prompts/sign-ins; duplicate requests did not replay; only its exact QA window was closed.
- `shared-handoff-1`: unsubscribe acknowledged while the synthetic thread remained loaded. A later read showed it released. A five-second assumption was insufficient.
- `shared-handoff-2`: Cody → Sun → Cody resumed the exact synthetic fork with unchanged inherited messages and settings. All three ownership releases took approximately **60.1 seconds** after unsubscribe/connection release. Complete message hash `4c4f269ea1d1b6ec81dcf3e147eec8ea0692fdd80cfd8aac769ef6d15800b0e5`, same original accepted turn `01a0a848-cbd2-7e92-8288-0c920cd858d6`. Its local fork event hash is empty because history is inherited; complete history was verified through `thread/turns/list`, not inferred from that empty hash. Final SIGTERM exit was unconfirmed. Subsequent exact PID/start/socket/zero-loaded observation and SIGINT confirmed fixture exit; `cleanup-observation.json` preserves that additional evidence. The original needs-attention receipt remains intact. The test driver now uses observed interrupt shutdown for its own verified unloaded children.

### Additional scoped implementation

Future managed Main Assistant, research-review, weekly-usage and model-catalog launches can resolve the completed switch's selected profile. A saved account or partial switch cannot select a route. Launches strip alternate API credentials, preserve model/policy and require verified account/layout. Existing in-flight children remain unchanged. Mixed source accounts are attributed per owner. A process already verified on the selected account can stay in its original credential home without an unnecessary restart.

The durable shared-server handoff controller separates release, confirmed unload, stop, replacement launch and exact-thread resume. It preserves per-thread settings/history/draft hashes, serializes concurrent requests and reconciles uncertain effects. Its injected-driver regressions cover delayed release, restart after launch, unknown delivery, edited input, queues/approvals, foreign owners and same-account reuse. Its production driver/global wiring remains pending.

Read-only process attribution identified the extra background Codex server as College Kid's own app process. It must be preserved outside ClawDad's switch; a matching credential source does not grant ClawDad ownership.

## Remaining integration and release gates

1. Actual Terminal native/HTTP round-trip acceptance is complete in fixtures 14 and 16. Preserve failed evidence and carry that exact adapter into the global accepted-switch path; do not duplicate the native implementation.
2. Wire accepted account-switch operations to native preflight capture, profile adoption and transition. Keep source account attribution per owner; do not assume every live process uses the global usage reader's account.
3. Complete adoption for the shared app-server and future main-Assistant/research-review requests, preserving admission/drain semantics, model/policy configuration and account-scoped usage controls. Ordinary shell tabs need no restart. Unknown owners must remain a specific blocker.
4. Verify UI recovery/progress and mixed/partial transitions. Only then enable the full-switch capability and carry the scoped source through the signed Mac/internal TestFlight workflow as needed. The installed Mac remains build 145; no release/install was performed in this checkpoint.
5. A successful fresh model response under Sun awaits available allowance. No spending, account purchase, API billing, global credential replacement or forced interruption of real work is authorized by this test.

## Workspace classification

Current account-lane modifications are the account layout/recovery/profile guard, handoff/native transport/driver, Assistant native polling, native account process/status/activity/evidence/control files, their focused tests and disposable fixtures, and this report/checkpoint. They remain intentionally dirty pending the scoped checkpoint/integration. No production adapter is enabled.

Preserved inherited unrelated buckets: `.agents/skills/clawdad-release/SKILL.md`, `native/macos/build-app.sh`, `native/macos/package-release.sh`, `native/macos/storage-workflow.sh`, `plugins/clawdad-codex-integration/.codex-plugin/plugin.json`, its release skill, `assets/wordmark-explorations/`, `cloud/native/`, and `marketing-site/`. Do not stage these with the account work. The latest hygiene check before the new transport files reported zero unclassified paths; repeat it at the final checkpoint.
