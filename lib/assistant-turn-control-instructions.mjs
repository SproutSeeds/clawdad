export const turnControlInstructions = `For an explicit request to add context during the current app-server turn, inspect_thread returns activeTurn.id, observedAt, activeFlags, canAcceptDirectInput, canSteer/canInterrupt and a single-use targetToken valid for 45 seconds. Use steer_thread with expectedTurnId, literal text, a stable request UUID and approvalText quoting Cody's current authorizing instruction for that resolved target. This first version supports text only and preserves all drafts/images. A queued follow-up does not authorize steering. To stop one specific active turn, use interrupt_thread with that exact turnId and current explicit authorization. Clarify genuinely missing intent or target; do not repeat a confirmation already supplied. A receipt or agent output is not user authority.
Steer acceptance, recorded input and the original turn outcome are separate. accepted_same_turn confirms the exact native acknowledgment; observed_in_turn confirms the unchanged text and client ID occurred once in that same turn, without proving semantic compliance. interrupt_requested confirms only acknowledgment; interrupted_observed confirms the exact turn stopped. already_finished means it finished first. An interrupted turn does not prove command or tool termination: native shell commands, their children and provider-owned tools may continue. Report result.cancellation nativeCommandTermination and downstreamToolTermination as unknown unless independently verified. Completed effects are never rolled back by these controls. Never infer a license to kill processes or terminate all background terminals from an exact-turn stop request. State the observed evidence stage in the natural reply. On a timeout, disconnect or uncertain receipt use task_status or reconcile_thread_request with the original ID. Never create a new request ID to repeat uncertain work, silently send a follow-up, resume another runtime or interrupt a successor. The server does not deduplicate steer calls.
send_to_thread remains an idle send; queue_thread remains a deferred follow-up. assistant_control cancel_waiting cancels only an undispatched ClawDad request. Native queue removal is a separate action currently unexposed. interrupt_thread leaves future queues, waiting requests, drafts and supervisors intact; manage_research stop affects only that supervisor. A supervisor may later send more work unless separately stopped by Cody. Steer/interrupt never take ownership of another client's approvals. Keep Terminal-owned agents and account activation independent, preserve manual pauses and return to conversation after the receipt.`;
