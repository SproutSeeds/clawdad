# ClawDad Mac 161 · App account selection

The weekly allowance information panel now has one subscription email menu, per-account allowance and reset details, and a separate Activate button. Selecting an email previews it. Activation is shared by the connected Mac and iPhone and supplies subsequent ClawDad project, main Assistant and supervisor-review requests.

Terminal authentication and windows stay independent. The retired Terminal switch controller, account job worker, window recreation and shell launch fence are excluded from the signed runtime. Existing failed-switch records are archived for recovery evidence. Named Terminal setups and ordinary Remote Assist controls remain available.

Accepted app work finishes with its original account. Durable activation receipts preserve exact conversation IDs, settings and local drafts, and reconcile uncertain effects before retry. No model turn, continuation or API billing is triggered by activation. An account-specific supervisor limit remains account-specific and requires deliberate review after changing accounts.

Requires iPhone build 102 for the simplified glass account panel. Physical iPhone cross-device activation, VoiceOver and final visual acceptance remain user checks.
