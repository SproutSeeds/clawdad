<Window
    x:Class="ClawDad.Windows.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    Title="ClawDad">
  <Grid Background="{StaticResource ClawDadBackground}">
    <Grid.RowDefinitions>
      <RowDefinition Height="36" />
      <RowDefinition Height="*" />
    </Grid.RowDefinitions>

    <Grid x:Name="TitleBar" Grid.Row="0" Background="#FF1B1B1B">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" />
        <ColumnDefinition Width="Auto" />
      </Grid.ColumnDefinitions>
      <TextBlock
          Text="ClawDad"
          Foreground="#FFFFF0D1"
          FontWeight="SemiBold"
          VerticalAlignment="Center"
          Margin="14,0,0,0" />
      <Border
          x:Name="RemoteAssistIndicator"
          Grid.Column="1"
          Visibility="Collapsed"
          Background="#FF102D19"
          BorderBrush="#FF4BDC71"
          BorderThickness="1"
          CornerRadius="8"
          Padding="10,3"
          Margin="0,3,142,3">
        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
          <Ellipse Width="8" Height="8" Fill="#FF4BDC71" />
          <TextBlock
              Text="Remote Assist active"
              Foreground="#FFB9F7C9"
              FontSize="12"
              FontWeight="SemiBold"
              VerticalAlignment="Center" />
          <Button
              Content="Stop"
              Padding="8,1"
              FontSize="11"
              Click="StopRemoteAssistButton_Click" />
        </StackPanel>
      </Border>
    </Grid>

    <controls:WebView2
        x:Name="WorkspaceWebView"
        Grid.Row="1"
        HorizontalAlignment="Stretch"
        VerticalAlignment="Stretch" />

    <Grid
        x:Name="LoadingSurface"
        Grid.Row="1"
        Background="{StaticResource ClawDadBackground}">
      <StackPanel
          HorizontalAlignment="Center"
          VerticalAlignment="Center"
          Spacing="18"
          MaxWidth="520">
        <Image Source="Assets/ClawDadWordmark.png" Width="280" Stretch="Uniform" />
        <ProgressRing x:Name="LoadingRing" IsActive="True" Width="42" Height="42" />
        <TextBlock
            x:Name="LoadingText"
            Text="Starting ClawDad on Windows..."
            Foreground="#FFFFF0D1"
            FontFamily="Cascadia Mono"
            FontWeight="SemiBold"
            TextAlignment="Center"
            TextWrapping="Wrap" />
        <Button
            x:Name="RetryButton"
            Content="Try Again"
            Visibility="Collapsed"
            HorizontalAlignment="Center"
            Click="RetryButton_Click" />
      </StackPanel>
    </Grid>

    <controls:WebView2
        x:Name="RemoteAssistWebView"
        Grid.Row="1"
        Width="1"
        Height="1"
        Opacity="0"
        IsHitTestVisible="False"
        HorizontalAlignment="Left"
        VerticalAlignment="Top" />
  </Grid>
</Window>
